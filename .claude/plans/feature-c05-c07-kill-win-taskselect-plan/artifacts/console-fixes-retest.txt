POST-FIX RETEST — live Roblox Studio, 2026-08-11. All four fixes re-tested against their own repros.
Config debug: Intermission/Duration/EndScreen = 8/45/6, Solo/Verbose/ForceAswang = true (reverted after).

================================================================================
FIX 1 (High) — a death that is not a kill no longer strands the player
================================================================================
BEFORE: same dead character for 64s across 3 rounds, `alive 1 · you: ALIVE` in every snapshot.
        (artifacts/console-finding2-no-respawn.txt)

AFTER — death during ACTIVE routes through MarkKilled:

  [FIX2] killing the Humanoid during ACTIVE — exactly what Escape > Reset does
  [RoundService] Killed 11461085874; 0 alive.
  [Client] Snapshot — ACTIVE round #1 · ... · alive 0 · you: GHOST (29s left)
  [FIX2] t+ 2s  Character=nil  same-as-dead-body=false
  ...  (Character stays nil — the dead body is cleared, not left lying) ...
  [FIX2] t+14s  Character=nil  same-as-dead-body=false

  `alive 0 · you: GHOST` is the whole fix. The player is no longer counted as a living survivor, so
  §4.8's attrition win can reach its threshold again.

BOTH OTHER BRANCHES CHECKED, because the risk of this fix is stranding someone bodiless:

  [FIX2b] recovered a body after the round ended: true          <- a GHOST gets a body back at LOBBY
  [FIX2b] killing the Humanoid outside ACTIVE (lobby death) — expect a NEW character, not a ghost
  [FIX2b] t+ 2s  Character=Demiurgos_18  isNewBody=true          <- lobby death respawns, not ghosts
  ...  (isNewBody=true throughout) ...

  So: die in a round -> GHOST, uncounted. Die in the lobby -> respawn. Round ends -> ghosts recover.
  No path leaves a player without a body.

================================================================================
FIX 6 (Low) — the winning kill's corpse is no longer destroyed by its own round end
================================================================================
BEFORE: clearCorpses ran on every non-ACTIVE phase, so MarkKilled -> EndRound -> ENDING destroyed the
        corpse `commitKill` had made moments earlier, while commitKill was still on the stack.
AFTER:  cleared on INTERMISSION/IDLE only. Tested with a stand-in part, which needs no second player:

  [FIX6] marker placed in workspace.Corpses during ACTIVE
  [FIX6] t+ 18s  marker present = true
  [RoundService] -> ENDING (6s)
  [FIX6] t+ 20s  marker present = true      <-- SURVIVES ENDING. This is the fix.
  [FIX6] t+ 22s  marker present = true
  [FIX6] t+ 24s  marker present = true
  [RoundService] -> INTERMISSION (8s)
  [FIX6] t+ 26s  marker present = false     <-- cleared at INTERMISSION, as intended

================================================================================
FIX 7 (Low) — degenerate tunables fail CLOSED (terminal, not Studio)
================================================================================
  lune run tests/kill-validation.test.luau
  PASS  kill-validation: 16 grid cells + 26 cases

  Five new cases: Range = -8, 0, math.huge and NaN all refuse; a NaN Cooldown is treated as still on
  cooldown. The infinite case CAUGHT A HOLE IN THE FIRST ATTEMPT — `not (range > 0)` let math.huge
  through and 25,000,000 <= inf granted a kill from 5000 studs. Guard is now `range > 0 and
  range < math.huge`, written as one negated conjunction so NaN fails it too.

================================================================================
FIX 5 (Medium) — check:secrecy now sees a typed-local payload (terminal)
================================================================================
Injected the real leak into the real file and watched the guard go red:

  local payload: Types.PlayerKilledPayload =
      { VictimUserId = victim.UserId, Position = position, KillerUserId = killer.UserId }

  FAIL  src/server/Services/MonsterService.luau:602 — :FireAllClients carries what looks like the
        Aswang's identity on an unnamed remote
  - secrecy: 1 finding(s)

  Before the fix this scanned CLEAN, for two independent reasons, both now closed: the rule read the
  argument TEXT (`payload`, seven characters) instead of resolving the declaration, and the SECRET
  regex matched no `Killer*` token at all. File restored after the test; `- secrecy: ok` again.

  check-secrecy self-tests: 25/25 (3 new, covering typed and untyped local payloads).
  npm run check:guards: 25 suites ok.
