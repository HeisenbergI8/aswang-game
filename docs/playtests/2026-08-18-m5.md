# Playtest — 2026-08-18 · N players · M rounds — ══ GATE 1 (C19 / M5) ══

**Build:** 9459349 · **Config changed since last session:** none (first session)

Fill this in DURING the session. A summary written the next day is not data.

## Config under test

Recorded so the session is reconstructable — every number in Appendix A is a starting value to tune.

| Knob | Value |
| --- | --- |
| `Round.Intermission` | 25 |
| `Round.Duration` | 420 |
| `Round.EndScreen` | 12 |
| `Monster.KillCooldown` | |
| `Monster.TransformTime` | |
| `Monster.KillRange` | |
| `Tasks.TotalRequired` | |
| `Salt.SpawnCount` | |
| `Debug.SoloTesting` | false |

## Numbers

```
Rounds played:
Players who wanted a 6th round:      / 6      <- THE M5 GATE
Side win rate:            survivors __ / aswang __
Rounds where a transform was witnessed:  __ / __
Rounds where salt hit:                   __ / __
First-objective time, new players:       __ s   <- the M6 gate, target < 60
Players who quit mid-session:            __
```

## Watch list — tick when seen, note the round

- [ ] A player standing still, looking around — the 80% failure (§10)
- [ ] Someone dying first and going quiet (§4.7)
- [ ] A round where nobody witnessed a transform (§4.3)
- [ ] Salt never thrown, or thrown and missed every time (§4.6)
- [ ] The Aswang winning by hiding rather than hunting (§2)
- [ ] Silence where accusation should be (§4.5)
- [ ] Anyone leaving mid-session — the strongest negative signal

## Verbatim

What they SAID, in the moment, in their words.

- "..." — what they were doing when they said it

## What I wanted to explain but didn't

Every entry here is an FTUE bug. Explaining it during the session would have hidden it.

-

## What I changed as a result

ONE knob. Two changes at once and the next session teaches nothing about either.

-

## What I am NOT changing yet

The thing I was tempted by, and why one session is not enough evidence.

-
