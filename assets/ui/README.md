# ui/

Icons that Roblox primitives genuinely could not do.

Try `Frame` + `UICorner` + `UIGradient` + `UIStroke` first — it covers essentially the whole HUD in the
spec, scales cleanly to every screen, and costs no download. 60% of players are on mobile, so that last
point is not a small one. See `ui-polish`.
